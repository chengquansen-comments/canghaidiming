{
  "attack": {
    "handler": "_effect_attack",
    "required_fields": [
      "damage"
    ],
    "description": "执行一次攻击解析。"
  },
  "buff_next_attack_damage": {
    "handler": "_effect_buff_next_attack_damage",
    "required_fields": [
      "amount"
    ],
    "description": "给下一次攻击附加伤害。"
  },
  "distance": {
    "handler": "_effect_distance",
    "required_fields": [
      "amount"
    ],
    "description": "直接调整当前距离。"
  },
  "draw": {
    "handler": "_effect_draw",
    "required_fields": [
      "amount"
    ],
    "description": "抽牌或补牌。"
  },
  "enemy_momentum": {
    "handler": "_effect_enemy_momentum",
    "required_fields": [
      "amount"
    ],
    "description": "调整敌方势。"
  },
  "flag": {
    "handler": "_effect_flag",
    "required_fields": [
      "flag"
    ],
    "description": "设置本回合标记。"
  },
  "guard": {
    "handler": "_effect_guard",
    "required_fields": [
      "amount"
    ],
    "description": "为玩家增加格挡。"
  },
  "momentum": {
    "handler": "_effect_momentum",
    "required_fields": [
      "amount"
    ],
    "description": "为玩家恢复或减少势。"
  },
  "set_distance_step_toward_preferred": {
    "handler": "_effect_set_distance_step_toward_preferred",
    "required_fields": [],
    "description": "将距离朝玩家优势距离推进 1。"
  },
  "set_distance_toward": {
    "handler": "_effect_set_distance_toward",
    "required_fields": [
      "target"
    ],
    "description": "将距离朝目标档位推进 1。"
  },
  "toggle_distance": {
    "handler": "_effect_toggle_distance",
    "required_fields": [],
    "description": "按当前切换方向调整 1 档距离。"
  }
}
