extends RefCounted

# Disabled placeholder.
# The previous clickable map controller extended NarrativeDemoController and caused
# Godot parser class resolution issues in Web builds. Keep this file inert so the
# project can compile while the clickable map logic is folded back into the base
# narrative_demo_controller.gd in a later safe patch.
